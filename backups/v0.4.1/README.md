# DrawOver v0.4.1 — backup snapshot

A known-good copy of the app at version 0.4.1, kept as a restore point.
`index.html` + `style.css` here are self-contained (the HTML references
`style.css` relatively), so this folder also works when served directly.

To restore: copy these two files back to the repo root.
